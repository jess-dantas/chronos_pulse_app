# Roadmap

O roadmap do produto é versionado no repositório do **backend** (fonte canônica):

- **`chronos-pulse` → [`ROADMAP.md`](https://github.com/jess-dantas/chronos-pulse/blob/develop/ROADMAP.md)**

Este app entrega a frente Flutter de cada requisito (R-series). Estado atual: `R27` a `R30` concluídos; `R31` (Portal da Transparência — aba **Portal Público** + `tenantSlug`) em andamento — backend pronto, frente Flutter entregue.

**Cobertura de testes do app: 176** (`flutter test`), analyze limpo.